server:
  port: ${PORT:8080}
spring:
  application:
    name: doan

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update


    show-sql: false

    properties:
      hibernate:
        format_sql: false

jwt:
  secret: ${JWT_SECRET:VGhpc0lzTXlTdXBlclNlY3JldEtleTEyMzQ1Njc4OTAxMjM0NTY=}

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:deddrlyag}
  api-key: ${CLOUDINARY_API_KEY:659455413236385}
  api-secret: ${CLOUDINARY_API_SECRET:Ta2N98tJx-8LXJja98AafUbSwIQ}